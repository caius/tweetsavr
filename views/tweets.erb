<ol>
  <% @tweets.each_with_index do |tweet, i| %>
    <li class="tweet-<%= i %><%= " original" if i == 0 %>">
      <span class="author"><%= tweet.user.name %></span>: 
      <span class="content"><%= tweet.text %></span>
    </li>
  <% end %>
</ol>
